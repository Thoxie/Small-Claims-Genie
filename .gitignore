# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Replit
.cache/
.local/

# Local environment files and database exports. Keep .env.example tracked.
.env
.env.*
!.env.example
*.dump
*.backup
*.sql.gz
database-data/
database-data-exports/

# Large video files (exceed GitHub 100MB limit — host on YouTube/CDN instead)
*.mp4
*.mov
*.avi
*.mkv

# Local backup archives (keep out of git)
backups/

# Large source archives — these bloat git history significantly
# Any .zip or .tar.* at any path is excluded; the attached_assets/ dir is also
# fully excluded below, so zip files uploaded as agent assets are covered too.
*.zip
*.tar.gz
*.tar.bz2
*.tar.xz

# Uploaded test assets and screenshots (not part of the app)
attached_assets/
screenshots/
.agents/agent_assets_metadata.toml
